Java 19
Main->Edit configurations->Modify options->VM options seçib
"--module-path "C:\Users\nijat.gurbanli\Downloads\javafx-sdk-21.0.9\lib" --add-modules javafx.controls,javafx.fxml"
əlavə edirsən javaFx üçün.

Table lar üçün model yazmamışam query create etmishem

user

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(100) NOT NULL,
    role VARCHAR(20) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

product

CREATE TABLE IF NOT EXISTS product (
    id SERIAL PRIMARY KEY,
    barcode VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    price NUMERIC(10,2) NOT NULL,
    stock INT NOT NULL
);

sales

CREATE TABLE IF NOT EXISTS sales (
    id SERIAL PRIMARY KEY,
    product_id INT REFERENCES product(id),
    quantity INT NOT NULL,
    total NUMERIC(10,2) NOT NULL,
    payment_type VARCHAR(20) NOT NULL,
    username VARCHAR  NOT NULL,
    sale_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

audit log

CREATE TABLE audit_log (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50),
    action VARCHAR(50),
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

userlər: nicat, ilham, emil
parol: 1

koddan exe file yaradıb production hala gətirmək üçün Inno Setup gui istifade et
və ya but file yarat içinde aşağıdakını istifadə et

@echo off
REM JavaFX POS tətbiqini run etmək üçün bat faylı

cd /d "C:\Users\nijat.gurbanli\Desktop\pos-system"

REM JavaFX SDK yerləşdiyi yolu göstərin
set JAVAFX_LIB=C:\Users\nijat.gurbanli\Downloads\javafx-sdk-21.0.9\lib

REM Tətbiqin JAR faylının yolu
set JAR_FILE=target\pos-system-1.0-SNAPSHOT.jar

REM Run əmrini işə salır
java --module-path "%JAVAFX_LIB%" --add-modules javafx.controls,javafx.fxml -jar "%JAR_FILE%"

pause


