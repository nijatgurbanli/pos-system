<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.pos.controller.MainController"
            prefWidth="900" prefHeight="600">

    <!-- 🔸 Yuxarı panel (header) -->
    <top>
        <HBox spacing="20" style="-fx-background-color: #2e86de; -fx-padding: 10;">
            <Label text="🛒 Market POS Sistemi"
                   style="-fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold;"/>
            <Pane HBox.hgrow="ALWAYS"/>
            <Label fx:id="cashierLabel"
                   text="Kassir: admin"
                   style="-fx-text-fill: white; -fx-font-size: 14px;"/>
            <Button text="Çıxış" onAction="#handleLogout" style="-fx-background-color: #c0392b; -fx-text-fill: white;"/>
        </HBox>
    </top>

    <!-- 🔸 Sol panel (məhsul əlavə etmək) -->
    <left>
        <VBox spacing="10">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <Label text="Məhsul əlavə et" style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
            <TextField fx:id="barcodeField" promptText="Barkod daxil et" prefWidth="200"/>
            <Button text="Əlavə et" onAction="#handleAddProduct" prefWidth="200"/>
        </VBox>
    </left>

    <!-- 🔸 Mərkəz (səbət cədvəli) -->
    <center>
        <TableView fx:id="cartTable" prefWidth="600">
            <columns>
                <TableColumn text="Məhsul" fx:id="nameColumn" prefWidth="200"/>
                <TableColumn text="Qiymət" fx:id="priceColumn" prefWidth="100"/>
                <TableColumn text="Miqdar" fx:id="quantityColumn" prefWidth="100"/>
                <TableColumn text="Cəmi" fx:id="totalColumn" prefWidth="100"/>
            </columns>
        </TableView>
    </center>

    <!-- 🔸 Aşağı panel (ümumi məbləğ və əməliyyatlar) -->
    <bottom>
        <HBox spacing="20" alignment="CENTER_RIGHT">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>

            <Label text="Ümumi məbləğ:" style="-fx-font-size: 16px; -fx-font-weight: bold;"/>
            <Label fx:id="totalLabel" text="0.00 ₼" style="-fx-font-size: 16px; -fx-text-fill: #27ae60;"/>
            <Button text="Satışı Təsdiqlə" onAction="#handleConfirmSale" style="-fx-background-color: #27ae60; -fx-text-fill: white;"/>
            <Button text="Təmizlə" onAction="#handleClear" style="-fx-background-color: #e67e22; -fx-text-fill: white;"/>
        </HBox>
    </bottom>

</BorderPane>
